name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g., v0.1.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Create tag for manual dispatch
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git tag -f ${{ github.event.inputs.tag }}
          git push origin ${{ github.event.inputs.tag }} --force

      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Remove clipboard dependency for release builds
        run: |
          cd cmd/glive
          # Remove clipboard from go.mod since we use noclipboard tag
          go mod edit -droprequire golang.design/x/clipboard
          go mod tidy -tags=noclipboard

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          version: '~> v2'
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
